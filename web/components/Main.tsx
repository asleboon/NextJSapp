import { Stack } from '@chakra-ui/core';

export const Main = (props: any) => (
    <Stack
        spacing="1.5rem"
        width="100%"
        maxWidth="48rem"
        mt="-45vh"
        pt="8rem"
        px="1rem"
        {...props}
    />
);
